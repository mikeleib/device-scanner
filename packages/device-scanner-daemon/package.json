{
  "name": "@mfl/device-scanner-daemon",
  "version": "0.0.0",
  "description": "A persistent process that consumes udev events over a unix domain socket.",
  "main": "dist/device-scanner-daemon",
  "scripts": {
    "prepublish": "old_pwd=$PWD; cd ../../; dotnet fable npm-run rollup --args \"-i $old_pwd/lib/server.fs -o $old_pwd/$npm_package_main\" --port 'free'; chmod +x $old_pwd/$npm_package_main"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": "https://github.com/intel-hpdd/device-scanner/tree/master/packages/device-scanner-daemon",
  "author": "MFL Team",
  "license": "MIT"
}
